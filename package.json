{
  "name": "todo-app-chatbot-phase-iii",
  "version": "0.1.0",
  "scripts": {
    "build": "echo 'Build step not needed for Python app'",
    "start": "uvicorn api.app:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {},
  "devDependencies": {}
}